# ATMStatusMonitoring
WPF application for tracking ATM status between bank departments

The application uses `Dapper` and a serviñe-based database `ATMDatabase` to store information about application users, ATMs and their statuses.
The database path is specified in `app.config` as `ATMDb`

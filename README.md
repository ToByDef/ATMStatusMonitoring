# ATMStatusMonitoring
WPF application for managing the state of ATMs and transferring this information between structural divisions of the bank.

The application uses the `Entity Framework Core`, the `Code First` approach and database `ATMDatabase` to store information about application users, ATMs and their status.

To build, you need to download packages from NuGet: `EntityFrameworkCore` and `EntityFrameworkCore.SqlServer`

--------------------------------------------------------------
WPF приложение дл€ оперировани€ состо€нием банкоматов и передачи этой информации между структурными подразделени€ми банка.

¬ приложении используетс€ `Entity Framework Core`, подход `Code First` и база данных `ATMDatabase` дл€ хранени€ информации о пользовател€х приложени€, банкоматах и их состо€ни€х.

ƒл€ сборки потребуетс€ загрузка пакетов из NuGet: `EntityFrameworkCore` и `EntityFrameworkCore.SqlServer`



